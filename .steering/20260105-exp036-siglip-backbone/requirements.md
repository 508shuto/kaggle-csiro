# Requirements

## 目的
DINOv3とは異なる事前学習を持つSigLIPをバックボーンとして使用し、アンサンブル用モデルを作成

## 変更/追加する機能
- exp028ベースで新規実験exp036を作成
- バックボーンをDINOv3からSigLIPに変更

## 制約・前提条件
- コード変更は最小限（config変更のみ）
- frozen backbone パターンを維持
- アンサンブル候補として多様性を重視（単体CVが多少下がっても可）
